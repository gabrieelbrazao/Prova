import { StyleSheet } from "react-native";

export default StyleSheet.create({
  topView: {
    alignItems: "center",
    paddingTop: 30,
  },
  textsView: {
    paddingTop: 30,
    alignItems: "center",
  },
});
